services:
  web:
    build: .
    container_name: turf-backend
    ports:
      - "8080:8080"
    environment:
      - DJANGO_SETTINGS_MODULE=turf_backend.settings_docker
      - DEBUG=True
      - DB_NAME=turf_db
      - DB_USER=postgres
      - DB_PASSWORD=Adugalam@1234
      - DB_HOST=34.14.169.159
      - DB_PORT=5432
    restart: unless-stopped
